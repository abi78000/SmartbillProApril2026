import { FocusOnKeyDirective } from './focus-on-key.directive';

describe('FocusOnKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnKeyDirective();
    expect(directive).toBeTruthy();
  });
});
